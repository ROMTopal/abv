import 'dart:ui';

class CategoryModel{
  String? icon;
  Color? bgColor;
  String? title;
  bool? isSelected;
  List<String>? keywords;

  CategoryModel({this.icon, this.bgColor, this.title, this.isSelected, this.keywords});
}

