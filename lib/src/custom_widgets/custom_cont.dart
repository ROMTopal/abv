import 'package:flutter/material.dart';

class CustomCont extends StatelessWidget {
  const CustomCont({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      width: double.infinity,
      child: Card(
      ),
    );
  }
}
